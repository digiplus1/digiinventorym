import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ftg.digixpress.digiinventory',
  appName: 'digiinventory',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
